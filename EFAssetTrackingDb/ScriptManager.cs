using Azure;

internal class ScriptManager
{
    internal static void RegisterStartupScript<T>(Page<T> page, object value, string v1, string v2, bool v3)
    {
        throw new NotImplementedException();
    }
}